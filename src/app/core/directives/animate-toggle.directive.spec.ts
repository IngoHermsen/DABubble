import { AnimateToggleDirective } from './animate-toggle.directive';

describe('AnimateToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimateToggleDirective();
    expect(directive).toBeTruthy();
  });
});
